import styled from "styled-components";

export const Wrapper = styled.div`
  h1 {
    font-size: 36px;
    font-weight: 700;
    color: var(--title)
  }
  h2 {
    font-weight: 700;
    font-size: 28px;
    line-height: 33px;
    color: var(--title)
    margin: 0;
  }
  h3 {
    font-weight: bold;
    font-size: 24px;
    line-height: 28px;
    color: var(--title);
    margin: 0;
  }
`;
